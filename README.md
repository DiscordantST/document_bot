# 📄 Documents Bot

Telegram бот для хранения и отслеживания документов с датами окончания.

## ✨ Возможности

- 📎 **Загрузка документов** с валидацией форматов
- 📅 **Отслеживание сроков** действия документов
- 📁 **Шаблоны** для группировки документов
- ⏰ **Напоминания** о документах с истекающим сроком
- 🔍 **Поиск** по документам
- 📊 **Статистика** по документам

## 🚀 Установка

### 1. Клонирование и зависимости

```bash
cd docs_bot
pip install -r requirements.txt
```

### 2. Настройка токена бота

Создайте бота через [@BotFather](https://t.me/BotFather) и получите токен.

```bash
export BOT_TOKEN="your_bot_token_here"
```

Или отредактируйте `config.py`:
```python
BOT_TOKEN = "your_bot_token_here"
```

Или создайте файл в корне проекта `.env`:
BOT_TOKEN = "your_bot_token_here"
### 3. Запуск

```bash
python bot.py
```

## 📁 Структура проекта

```
docs_bot/
├── bot.py              # Точка входа, инициализация
├── config.py           # Конфигурация и константы
├── database.py         # Работа с SQLite
├── utils.py            # Вспомогательные функции
├── jobs.py             # Фоновые задачи (напоминания)
├── handlers/
│   ├── __init__.py
│   ├── commands.py     # Обработчики команд
│   ├── documents.py    # Работа с документами
│   ├── templates.py    # Работа с шаблонами
│   └── callbacks.py    # Общий callback dispatcher
├── requirements.txt
└── README.md
```

## 🤖 Команды бота

| Команда | Описание |
|---------|----------|
| `/start` | Начать работу с ботом |
| `/mydocs` | Показать список документов |
| `/templates` | Управление шаблонами |
| `/cancel` | Отменить текущую операцию |
| `/help` | Справка |


## 📎 Загрузка документа

1. Отправьте файл боту
2. Введите название документа
3. Выберите дату начала действия
4. Выберите дату окончания (опционально)
5. Выберите шаблон (опционально)

### Поддерживаемые форматы

- **Документы:** PDF, DOC, DOCX, XLS, XLSX, PPT, PPTX, ODT, ODS, ODP, RTF, TXT
- **Изображения:** JPG, JPEG, PNG, GIF, WEBP
- **Архивы:** ZIP, RAR, 7Z

## 🔔 Напоминания

Бот автоматически отправляет напоминания об окончании срока действия документа:
- За 30 дней до окончания
- За 7 дней до окончания
- За 1 день до окончания


## 📊 Лимиты

| Параметр | Значение |
|----------|----------|
| Документов на пользователя | 100 |
| Шаблонов на пользователя | 20 |
| Максимальный размер файла | 20 МБ |

## 🔧 Конфигурация

Все настройки находятся в `config.py`:

```python
# Разрешённые форматы
ALLOWED_EXTENSIONS = {".pdf", ".doc", ".docx", ...}

# Максимальный размер файла
MAX_FILE_SIZE = 20 * 1024 * 1024

# Дни для напоминаний
REMINDER_DAYS_BEFORE = [30, 7, 1]

# Лимиты
MAX_DOCUMENTS_PER_USER = 100
MAX_TEMPLATES_PER_USER = 20
```

## 📝 Лицензия

MIT
